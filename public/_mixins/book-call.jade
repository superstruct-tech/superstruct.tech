mixin bookCall
  - id = Math.floor(Math.random() * 1e16).toString(36)
  - buttonId = id + "button"
  .content
    ul.actions
      li
        a.button.primary.large(id=buttonId, href="https://harmonizely.com/david-guttman/superstruct-15", target="_blank") Book a 15 minute Call with David

    p
      iframe(id=id, src="https://harmonizely.com/david-guttman/superstruct-15", style="width:100%; height:650px; display: none", frameborder="0", scrolling="yes")

      script.
        ;(function () {
          const btn = document.querySelector('##{buttonId}')
          const iframe = document.querySelector('##{id}')
          btn.addEventListener('click', function (evt) {
            if (window.innerWidth < 925) return
            evt.preventDefault()
            btn.style.display = 'none'
            iframe.style.display = 'block'
          })
        })()
        

    p (Don’t worry, this isn’t a high pressure sales call. I just like to make sure we’re a good fit before working together.)
